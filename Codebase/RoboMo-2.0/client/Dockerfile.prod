# Production build - uses pre-built artifacts
FROM nginx:alpine

# Copy the build folder from the host machine (must run npm run build locally first)
COPY build /usr/share/nginx/html

# Copy custom nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
EXPOSE 443

CMD ["nginx", "-g", "daemon off;"]
